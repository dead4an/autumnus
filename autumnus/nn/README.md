# 🧠 NN (Neural Networks)

NN module is designed to implement various neural network layers. These layers operate on Tensors so backpropagation process is automated so different layers could be combined in different ways without worring about gradients.
